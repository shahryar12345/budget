const initialState = {
  isLoading: false,
  isLoaded: true,

  ItemMonths: [],

  LastUpdate: "",

  count: 1,
};

const MDItemMonthsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default MDItemMonthsReducer;
